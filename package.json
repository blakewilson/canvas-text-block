{
  "name": "canvas-text-block",
  "version": "0.0.1",
  "description": "Easily render a wrapping block of text in an HTML Canvas Element.",
  "main": "dist/index.js",
  "scripts": {
    "test": "jest",
    "dev": "ncc build ./src/canvasTextBlock.ts --minify --out dist --target es2015 --watch",
    "build": "ncc build ./src/canvasTextBlock.ts --minify --out dist --target es2015",
    "format": "prettier --write ./src"
  },
  "keywords": [
    "canvas",
    "fillText"
  ],
  "author": "Blake Wilson (https://blake.id)",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "@types/jest": "^26.0.22",
    "@types/node": "^14.14.37",
    "@vercel/ncc": "^0.27.0",
    "canvas": "^2.7.0",
    "jest": "^26.6.3",
    "prettier": "^2.2.1",
    "ts-jest": "^26.5.4",
    "typescript": "^4.2.3"
  }
}
